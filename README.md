# PointOfSales
“PointOfSales App” is software designed to help stores manage operational needs such as transactions, inventory, and capital. The app is built using FastAPI as the backend to provide the API, and a Flutter-based mobile app as the frontend. The system also includes several user roles to support store management.
